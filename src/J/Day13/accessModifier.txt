Private - Access level only within class, Cannot be accessed outside the class.
Default - Access level only within package, Cannot be accessed outside the package.
Protected - Access level within the package and outside the package only through child class.
Public - Access level is everywhere i.e. within the class, outside the class and within the package & outside the package.
 encapsulation is wrapping up data into single unit is called encapsulation.
